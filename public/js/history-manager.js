// Gestionnaire de l'historique des stocks
console.log("History manager loaded");

// Cette fonction peut être vide pour le moment, juste pour éviter les erreurs
function initHistoryManager() {
  // Sera implémentée selon les besoins
}

// Initialiser au chargement du document
document.addEventListener('DOMContentLoaded', function() {
  initHistoryManager();
}); 